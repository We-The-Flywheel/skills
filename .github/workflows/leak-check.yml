name: leak-check

on:
  push:
  pull_request:

jobs:
  scrub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run scrub-check
        run: bash scripts/scrub-check.sh
